export interface BufferedRange {
  id: string
  start: number
  end: number
}

export interface FileFoundResponse {
  found: boolean
}

export interface MediaFilePath {
  id: string
  name: string
  path: string
}